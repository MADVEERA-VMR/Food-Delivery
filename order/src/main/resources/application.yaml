spring:
  application:
    name: ORDER-SERVICE
  datasource:
    url: jdbc:mysql://localhost:3307/orderdb
    username: root
    password: fj68JGI2!qp79.,
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

server:
  port: 9094

services:
  user:
    base-url: http://USER-SERVICE:9093/user/fetchUserById/

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
